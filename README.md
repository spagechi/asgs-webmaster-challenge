# asgs-webmaster-challenge

**Instructions for setting up and running project**
1. First, cd into the client directory. `cd client`
2. Install the project's packages. `npm install`
3. Run the dev server. `npm run dev`
